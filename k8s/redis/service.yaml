apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: production
  labels:
    app: redis
spec:
  selector:
    app: redis
  ports:
    - port:       6379
      targetPort: 6379
      protocol:   TCP
  type: ClusterIP   # Internal only
