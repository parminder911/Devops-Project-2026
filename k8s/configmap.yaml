apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: production
data:
  REDIS_HOST:  "redis-service"
  REDIS_PORT:  "6379"
  DB_HOST:     "postgres-service"
  DB_PORT:     "5432"
  APP_ENV:     "production"
  LOG_LEVEL:   "INFO"
