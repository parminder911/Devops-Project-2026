apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: production
type: Opaque
# stringData is plain text — Kubernetes auto-encodes to base64 internally
# Never commit real passwords to Git. These are set once on the cluster.
stringData:
  DB_NAME:     "hudocafedb"
  DB_USER:     "hudocafe_user"
  DB_PASSWORD: "HudoCafe@Secure2026!"
